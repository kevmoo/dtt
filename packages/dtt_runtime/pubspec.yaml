name: dtt_runtime
description: "Core runtime event parsing and Shelf-based routing engine for Dart Terraform Triggers (dtt)."
version: 0.1.0-wip

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  async: ^2.11.0
  google_cloud_shelf: ^0.5.0
  protobuf: ^6.0.0
  shelf: ^1.4.0
