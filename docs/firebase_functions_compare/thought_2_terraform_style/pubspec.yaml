name: thought_2_terraform_style
description: "Thought 2: Deploying event-driven triggers natively on GCP using a static Shelf server and Terraform."
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.0
  protobuf: ^6.0.0
