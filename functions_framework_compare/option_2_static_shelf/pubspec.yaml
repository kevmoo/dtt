name: option_2_static_shelf
description: "Option 2 (RECOMMENDED): Reimagining FaaS triggers with a zero-reflection, static Shelf micro-engine."
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.0
  protobuf: ^6.0.0
  google_cloud: ^0.5.0 # For GCP logging and metadata server
